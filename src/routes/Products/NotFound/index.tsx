import './styles.css';

export default function NotFound({ content_page }: any) {

    return (

        <main>
            <div className="not-found">
                <h1>{content_page = "Não encontrado!"}</h1>
            </div>
        </main>
    );
}