import './styles.css';

export default function About({ content_page }: any) {

    return (

        <main>
            <div className="about">
                <h1>{content_page = "Sobre nós"}</h1>
            </div>
        </main>
    );
}